[package]
name = "result_utils"
version = "1.2.0"
edition = "2024"

[features]
default = []
tracing = ["dep:tracing"]

[dependencies]
tracing = { version = "0.1", optional = true }

[dev-dependencies]
result_utils = { path = ".", features = ["tracing"] }

